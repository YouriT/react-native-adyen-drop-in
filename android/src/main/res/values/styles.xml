<resources>

    <style name="Adyen.Pay.Button.Colored" parent="Widget.AppCompat.Button.Colored">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/pay_button_height</item>
        <item name="android:layout_marginBottom">@dimen/standard_half_margin</item>
        <item name="android:layout_marginLeft">@dimen/standard_margin</item>
        <item name="android:layout_marginRight">@dimen/standard_margin</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:theme">@style/Adyen.Pay.Button</item>
    </style>

    <style name="Adyen.Pay.Button" parent="Theme.AppCompat.Light">
        <item name="android:buttonStyle">@style/Widget.AppCompat.Button.Borderless.Colored</item>
        <item name="colorAccent">@color/payPrimaryColor</item>
    </style>

</resources>
